# 2026-03-17 — Daily Log

## Inbox Zero (Morning — ~9:27 AM)
- 15 emails processed
- Auto-deleted: Calendar "no events", Duolingo, Kraken promo, Champions Green, Papa Johns, 2x SABESP/GA Filing (afternoon)
- **Tracked:** OLS Stone Center Quote #QUO59788 — $1,419.97 (exp Apr 15) → task created
- **Tracked:** Thompson Quote — Bluestone 600sqft + 40lnft edge (pickup Mar 20) → task created
- Archived: Heritage auto-reply (Marc out of office)
- Deleted: BofA alert ($256 withdrawal — Regis confirmed ok), OneDrive memories (5 photos sent to Telegram first), Google review (Lisa Bridges 5★ Tucano), Search Console (apluspowercleaning validation), Day Ahead digest, Google personal info, Jobber newsletter, Google Ads invite, Goodreads

## Obsidian MCP Setup (10:00–11:00 AM)
- **Goal:** Replicate TARSON graph nodes into Obsidian for trial comparison
- Obsidian already installed on ubuntoris (`~/.local/bin/obsidian`, v1.12.4)
- Created new vault at `/srv/nas/tarson-vault/`
- Local REST API plugin installed manually (v1.x, port 27124 HTTPS)
- Issue: Obsidian safe mode blocked plugin on first launch
- Fix: set `safeMode: false` in app.json + set vault `"open": true` in `~/.config/obsidian/obsidian.json`
- Obsidian launched with `XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.CV00L3 DISPLAY=:0 ... --disable-gpu`
- REST API confirmed working: `curl -sk -H "Authorization: Bearer tarson_obsidian_key_2026" https://localhost:27124/`
- Installed `obsidian-mcp` npm package → `/home/regis/.npm-global/bin/obsidian-mcp`
- MCP server configured in `/home/regis/.openclaw/workspace/config/mcporter.json`
- Note: OpenClaw config schema does NOT have a `tools.mcp` section — MCP served via mcporter only

## TARSON → Obsidian Sync
- Script: `scripts/sync_nodes_to_obsidian.py`
- **100 nodes synced** (50 email, 33 task, 15 event, 1 job, 1 note)
- Notes organized in folders: `Emails/`, `Tasks/`, `Events/`, `Jobs/`, `Notes/`
- **Wikilinks added** connecting related nodes:
  - Jobs → related quote emails (Heritage, OLS, GLM, Luxury Landscape)
  - Tasks ↔ emails by category (ACTION, BILL, QUOTE, etc.)
  - Same-supplier cross-links
  - Gmail thread ID links
- Index files created: `Emails/_Index.md`, `Tasks/_Index.md`, etc.
- Home dashboard: `TARSON_Home.md` with overdue, due today, upcoming, open sections
- **299 total .md files** in vault
- Next step pending: systemd service for Obsidian auto-start

## Lessons / Notes
- Obsidian on Wayland/Ubuntu needs: `--no-sandbox --disable-gpu` + correct XAUTHORITY
- `obsidian.json` vault must have `"open": true` for the correct vault to load on startup
- REST API plugin API key is set manually in `data.json` (not generated by UI since we bypassed UI setup)
- Regis wants green light rule enforced strictly — don't proceed without explicit confirmation
