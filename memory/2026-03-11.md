# Daily Memory — Wednesday, March 11, 2026

## 6:33 AM — Morning Session

### TARSON Graph — Node Enrichment (6:03–6:20 AM)

**Context:** Regis asked to "improve the connection between nodes" (TARSON Graph on Oracle Cloud, not OpenClaw nodes).

**State found:**
- 59 nodes, 19 edges — graph was technically connected but semantically blind
- All 17 email nodes named "Email 19abc..." (useless)
- Calendar events: 15 nodes, 0 edges (completely isolated)
- 5 tasks with no connections at all

**What was done (`scripts/enrich_graph.py`):**
1. **Renamed all 17 email nodes** with real Gmail subjects (e.g., "Your Sawnee EMC bill is ready", "Novo SSN", "Sales Order #SO186606")
2. **Linked calendar events** — Mojitos events → Mojitos task; all birthdays/anniversaries → person_regis; Tucano webinars → company_tucano
3. **Anchored orphaned tasks** — SABESP/JMJ/Bon Jovi → person_regis; Jobber/GA Registration/Crown Terrace → company_tucano; Crown Terrace order → job_crown_terrace (part_of)

**Result:** 19 → 46 edges. Graph now tells a coherent story.

**Also discovered:** stats API was showing stale count (19) even after edges existed in SQLite. It corrected after the enrichment run. Possibly a caching issue in the API.

---

### Architecture Insight — Status-Driven Nodes (6:20 AM)

**Regis proposed a fundamental rethink** of the graph schema:

> "If each element is a node and we can connect them freely, we don't need specifically to connect an email to a task. The email itself is a node with a field status, so a task by itself."

**The model:**
- Every node can have a `status` field → makes it an action item
- Hierarchy is defined by edges (`part_of`, `child_of`) → determines task vs sub-task vs project
- No artificial type separation between "email" and "task" — same entity, different status/tags
- Eliminates current duplication: email node + task node for same thing becomes ONE node

**Example hierarchy:**
```
[job] Crown Terrace  status:open
  └─ [order] OLS Stone Center  status:placed
  └─ [quote] GLM  status:received
  └─ [payment] Tom McKinstry  status:done
```

**TARSON assessment:** Architecturally sound — this is how Tana/Notion/Roam work. Reduces duplication, more flexible. Suggested:
1. Add `status` + `due_date` fields to node schema
2. Remove email→task pairs, keep one enriched node
3. Dashboard "task view" = filter by `status != null AND status != done`
4. Rebuild now (59 nodes is nothing, retrofitting at 5000 nodes is painful)

**Decision:** Not yet acted on — Regis raised it, TARSON responded positively. **Next step: schema redesign.**

---

### System Issues Noted

- **Memory search (semantic):** DOWN — Gemini API key invalid. `memory_search` tool unavailable. Workaround: read files directly.
- **MacMini node:** Connected, running older agent v2026.2.12 (main system: v2026.3.2)
- **WindowsPC node:** Offline

---

### Heartbeat — 6:30 AM
- 6 unread emails (Duolingo auto-ignored)
- Two Tucano webinars TODAY: Hardscaper noon EDT, Techo-Bloc 1 PM EDT
- No critical tasks. Xfinity $10 auto-pay Mar 14 (3 days).

---

### TARSON Graph — Phases 1 & 2 Complete (Mar 11 AM)

**Phase 1** (Claude Code `dawn-ocean`): Schema rebuild — status/due_date fields, D3 dropped, inbox/table/kanban UI deployed.

**Phase 2** (Claude Code `tidal-crest`): Supertag sidebar, full CRUD, AI capture endpoint, keyboard shortcuts (⌘K), toast notifications, command palette. Deployed to Oracle.

**Known pending:**
- 🔑 **Anthropic API key invalid** — AI capture endpoint returns 401. The `ANTHROPIC_API_KEY` on ubuntoris is expired. Regis will provide a new key when ready. Oracle is configured correctly (EnvironmentFile in systemd, .env file in place) — just needs a valid key dropped in.
- 📱 **Mobile responsiveness** — Claude Code session `tidal-crest` building this now.

**Phase 3 ideas discussed:**
- Telegram → graph capture (send URL/voice/image to TARSON → auto-classifies → pushes to dashboard)
- Whisper transcription for voice fragments

---

## 10:33 AM — Mid-Morning Review

### TARSON Graph Phase 1 & 2 — DEPLOYED ✅

**Phase 1** (Claude Code `dawn-ocean`, ~7:22 AM):
- Schema: added `status` + `due_date` fields to all nodes
- D3 force-directed graph REMOVED entirely
- New UI: Inbox (urgency-grouped) | Table (sortable/filterable) | Kanban (drag status columns)
- Detail slide-in panel with inline editing
- Dark theme (#0f0f1a, #7c3aed accent)
- Data migration: 22 task nodes → status="open", 17 merged into email nodes
- Final: 42 nodes, 22 with status

**Phase 2** (Claude Code `tidal-crest`, ~8:19 AM):
- Left sidebar with supertags: 📥 Inbox | 📧 Email | ✅ Tasks | 📅 Calendar | 🗂️ Fragments | 🏷️ Tags
- Badge counts auto-refreshing every 30s
- Full CRUD: Quick Capture modal, inline edit, delete with confirmation, edge management
- New API endpoints: GET /api/supertags, POST /api/capture, cascade DELETE
- Keyboard shortcuts: ⌘K palette, N=new, Esc=close, ?=help
- Toast notifications, recently viewed, breadcrumbs
- ⚠️ AI capture endpoint returns 401 — ANTHROPIC_API_KEY on ubuntoris is expired. Regis said "keep it pending."

**Mobile responsiveness** — injected into tidal-crest session mid-build. Status: unknown (session still running or just finished). Need to verify.

**Live at:** http://129.213.125.244/graph/

### Architecture Decisions (Permanent)
- **Status-driven nodes**: every node can have `status` → makes it actionable (no separate "task" type)
- **Hierarchy via edges**: `part_of`, `child_of` defines project vs task vs sub-task
- **Graph = data model; UI = inbox/table/kanban** (graph visualization only in detail panel for relations)
- **Supertags = smart queries**, not folders — Email supertag = `source LIKE 'gmail:%'` etc.

### Inbox Zero — 10:02–10:19 AM
12 emails processed:
- **Auto-deleted** (silent): Kraken BTC, Duolingo, self-sent "Day Ahead" digest
- **OneDrive memories** — 5 photos sent to Telegram, then deleted ✅
- **USPS Informed Delivery** — 1 mail piece today, no images in email → deleted
- **National Life Group** — policy XXXXX7186 auto-draft **Mar 15** → 📌 **TRACKED** as [BILL]
- **PayPal** — card ending 5074 expiring → deleted (Regis chose not to track; likely will update manually)
- **Coinbase** — 2 BTC sweepstakes promo → deleted
- **Google Calendar notifications** (3) — Daily Agenda + 2 webinar reminders → batch deleted
- **Jobber marketing** — Marketing Suite upsell → deleted

### Open Reminders / Pending
- 🔑 **Anthropic API key**: expired on ubuntoris. When Regis provides new key → update Oracle /home/tarson/graph/.env + restart service
- 💳 **PayPal card 5074**: expiring, Regis deleted email. May need to update PayPal when new card arrives.
- 📱 **Mobile responsiveness**: verify tidal-crest session completed and deployed

---

## 12:32 PM — Pre-compaction Flush

### Inbox Zero Round 2 — In Progress (12:31 PM)
3 new emails fetched:
1. **Google Local Services** (19cdd9c94bd96add) — new call lead at 11:54 AM, customer ID 570-897-5121. Presented to Regis with Track/Archive/Delete buttons. **AWAITING RESPONSE** (msg 4028).
2. **Home Depot Pro Xtra** (19cdd7e49cc277ca) — February 2026 Pro Xtra statement
3. **Lowe's Pro** (19cdd61d68f2c0f5) — privacy statement update (noise)

### TARSON Graph — Mobile Responsiveness
Claude Code session `tidal-crest` was still running as of ~10:33 AM. Haven't confirmed deployment. Check http://129.213.125.244/graph/ on mobile to verify.

### Key Pending Items (as of 12:32 PM)
- 🔑 Anthropic API key expired — AI capture on TARSON graph needs new key from console.anthropic.com
- 💳 PayPal card 5074 expiring — Regis deleted email, likely updating manually
- 📌 National Life Group policy XXXXX7186 — auto-draft Mar 15 (tracked in TARSON-Tracking)
- 🟠 Xfinity $10 auto-pay — Mar 14 (3 days, in TARSON-Tracking)
- 📞 Google Local Services lead — awaiting Regis decision on tracking

---

## 2:33 PM — Afternoon Memory Flush

### Inbox Zero — Completed (rounds 2 & 3, ~12:31–14:18 PM)
All emails cleared:
- Google Local Services lead (11:54 AM call) → deleted (Regis chose not to track)
- Home Depot Pro Xtra Feb statement → deleted
- Lowe's Pro privacy update → deleted
- Apple iPhone 17e marketing → deleted
- Slack Tana Community digest → deleted
- ✅ Inbox zero achieved

### Anthropic API Key Reminder
- Regis replied to old message asking to "bring this topic back"
- Set cron reminder for **8 PM tonight** to prompt Regis to get new key from console.anthropic.com
- Cron ID: 5e8ef037-53dc-4214-801f-d3a45e62d617

### TARSON Graph — Status
- Phase 1 + Phase 2 fully deployed on Oracle
- AI Capture blocked (expired API key) — reminder set for 8 PM
- Mobile responsiveness: tidal-crest session last seen running ~10:33 AM; unconfirmed if deployed
- Live: http://129.213.125.244/graph/
